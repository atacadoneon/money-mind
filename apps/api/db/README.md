# Database migrations (API)

**Fonte única:** todas as migrations SQL ficam em `db/migrations/` na raiz do monorepo.

Execute `pnpm db:migrate` a partir da raiz (não há migrations TypeORM ativas em `src/database/migrations/`).
